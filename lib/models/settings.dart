import 'package:flutter/cupertino.dart';

class Settings {
  final String title;
  final String sub;
  final IconData icon;
  final Color color;

  Settings(this.title, this.sub, this.icon, this.color);
}
